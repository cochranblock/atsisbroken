# atsisbroken — Proof of Artifacts

Verifiable evidence behind `TIMELINE_OF_INVENTION.md`. Every entry
reproduces from the named commit.

## Commit ledger (recent)

| Date | Hash | Subject |
|---|---|---|
| 2026-05-03 | `e09cf05` | Initial scaffold (Path B, since superseded) |
| 2026-05-03 | `f495a88` | Provenance docs + exopack TRIPLE SIMS gate |
| 2026-05-03 | `aa93f81` | Strategy ladder + 156-test coverage push |
| 2026-05-03 | `6eed497` | TUI as default surface (Claude Code aesthetic) |
| 2026-05-03 | `9ebd1ad` | Phase 1 ship — persist queue + install-bridge + init parser |
| 2026-05-03 | `72ac7a9` | browser_detect + --profile flag + is_meaningfully_populated |
| 2026-05-03 | `0386909` | Mode::Shadow + Chrome extension ↔ desktop bridge |
| 2026-05-03 | `aacb982` | Doc structure aligned with Cochran Block convention |
| 2026-05-03 | `b0c7120` | PLAN.md — engineering + interested-parties funnels |
| 2026-05-03 | `0cebacf` | ATS fixture real-source attribution + kova exopack capability |
| 2026-05-03 | `da59c76` | PERFECTION_PLAN.md — 11 R-phase research contract |
| 2026-05-05 | `67e7cd3` | R1 done — HN cohort ATS market census |
| 2026-05-05 | `a008ffc` | R1-broader F500 reconciliation + R2-Ashby first capture |
| 2026-05-05 | `695a1d8` | **KEYSTONE: cmd_run actually fills forms via CDP** |
| 2026-05-05 | `45fa177` | **Mode persistence — Config + working `graduate`** |
| 2026-05-05 | `f413829` | **Mode-aware run loop — TrainingWheels prompts + Shadow gating** |

## Build verification (commit `f413829`, on n2/bt)

```
$ cargo build
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.18s
```

## Exopack TRIPLE SIMS gate

`cargo test --workspace` × 3, byte-identical normalized output
(after `sed 's/finished in [0-9.]*s/finished/'` to strip cargo's
wall-clock timing from the live e2e test result line; the e2e
test takes 1.3±0.05 s and the variance is harmless).

| Sim | SHA-256 |
|---|---|
| 1 | `c1d6d90eb930471dae700ae88c963416bc3d940e7dd0a4ffe12d6edd87852f30` |
| 2 | `c1d6d90eb930471dae700ae88c963416bc3d940e7dd0a4ffe12d6edd87852f30` |
| 3 | `c1d6d90eb930471dae700ae88c963416bc3d940e7dd0a4ffe12d6edd87852f30` |

**247/247 tests pass on every sim:**
- 219 in `atsisbroken` lib — schemas, queue, bridge, paths, resume,
  strategy, cdp, tui, browser_detect, **config (8 tests),
  run_loop::decide (10 tests, mode-aware policy)**.
- 11 in `atsisbroken` bin — parse_mode / parse_strategy_override.
- 1 in `tests/ats_e2e.rs` — **real chromium against 5 mock ATS
  fixtures generated by `kova::exopack::ats_fixtures` (Greenhouse,
  Lever, Workday, iCIMS, Ashby). Snapshot via `page.evaluate`,
  classify, fill, screenshot.** Live test caught the substring-
  `tel`-inside-`websiteLinkedIn` regression that no unit test
  would have surfaced.
- 9 in `tests/cli_smoke.rs` — `--help`, `--version`, init↔status,
  speak, bookmarklet, run-without-init, copy unknown key, cdp-probe.
- 7 in `atsisbroken-android` lib — JNI surface JSON shapes.

Reproduce:
```sh
for i in 1 2 3; do
  cargo test --workspace --quiet 2>&1 \
    | grep -E "^test |result:" \
    | sed 's/finished in [0-9.]*s/finished/' \
    | sort | sha256sum
done
```

## End-to-end smoke verifications (today, on dev box)

### Real CDP fill loop (`695a1d8`)
```
$ atsisbroken init <<< "Jane Q. Doe / jane.doe@example.com / +1-555-010-2030 / linkedin.com/in/janedoe / https://github.com/janedoe"
$ atsisbroken run --url file:///tmp/form.html
  url: file:///tmp/form.html
  fields seen: 6
  filled: 6
  skipped: 0
  feedback queue: 6 events on disk
  screenshot: ~/.atsisbroken/run-1778009681.png
  ⚠ NOT submitted — review in browser, click Submit yourself.
```
Screenshot stored, feedback.jsonl has 6 verbatim Feedback events.

### Mode persistence (`45fa177`)
```
$ atsisbroken graduate --yes  # TrainingWheels → Shadow ✓
$ atsisbroken graduate --yes  # Shadow → Chaos ✓
$ atsisbroken graduate --yes  # error: already at Chaos — pass --back to step down
$ atsisbroken graduate --back --yes  # Chaos → Shadow ✓

$ cat ~/.atsisbroken/config.toml
mode = "shadow"
confidence_threshold = 0.85
[feedback_delivery]
kind = "local_only"
```

### Mode-aware run loop (`f413829`)
```
$ HOME=/tmp/atsib_smoke_training_wheels atsisbroken run --url file://...
  mode: TrainingWheels
  field "Email" → email = "jane@example.com" — fill? [y/N]
  field "LinkedIn" → linkedin = "linkedin.com/in/jane" — fill? [y/N]
  filled: 1, skipped: 2, feedback queue: 2 events on disk
  ⚠ NOT submitted

$ HOME=/tmp/atsib_smoke_shadow atsisbroken run --url file://...
  mode: Shadow (confidence threshold: 0.85)
  filled: 2, skipped: 1
  ⚠ NOT submitted

$ HOME=/tmp/atsib_smoke_chaos atsisbroken run --url file://...
  mode: Chaos
  filled: 2, skipped: 1
  ⚠ NOT submitted
```

## Network surface

Crate dependency tree contains no HTTP client outside `chromiumoxide`'s
local CDP WebSocket. No `reqwest` / `hyper` / `ureq` / `surf`. Audit
in one `cargo tree`:
```sh
cargo tree --all-features | grep -iE "(reqwest|hyper|ureq|surf|tokio-tungstenite)"
# tokio-tungstenite is pulled by chromiumoxide for the local CDP socket — that's it.
```

## Honest scope of "shipped" today

What ACTUALLY works end-to-end on the dev box:
- `init` parses pasted resume → profile.toml ✓
- `--profile <path>` works on every subcommand that touches profile ✓
- `status` reads persisted mode + browser detection ✓
- `graduate [--yes] [--back]` flips mode + persists ✓
- `run --url <ATS-URL>` → launches chromium → fills → screenshots → persists feedback ✓
- TrainingWheels prompts y/N per field ✓
- Shadow gates by confidence (keyword=1.0 always passes today; structural correctness pinned) ✓
- Chaos fills everything classified ✓
- C1 enforced — never submits ✓

What's coded-but-not-yet-end-to-end-tested with a live external system:
- Native Messaging bridge — protocol unit-tested; hasn't been spoken to by a real Chrome session.
- `install-bridge` — writes the manifest; not verified with a real Chrome talking to a real binary.

What's plan-only (in `PLAN_*.md` docs):
- Trained classifier (R4)
- GitHub composer
- Profile expansion 11 → 75
- Custom regex hooks
- Multi-page Workday wizard navigation
- Workday re-render verifier
- TUI Browse tab

## Repository

- URL: https://github.com/cochranblock/atsisbroken
- Visibility: PUBLIC
- Default branch: `master`
- Most recent commit: `f413829`

## Cross-repo honesty

`kova` (sibling repo at `/home/mcochran/kova`) has 3 local commits
unpushed to a kova remote:
- `(blake3 fix)` — pre-existing
- `69c5002` — `exopack: ats_fixtures capability` (this session)
- `fbc6631` — `exopack/ats_fixtures: render_ashby rewritten from R2 live capture`

A fresh clone of kova to a different machine **will not build** until
those commits land in a kova remote. Push needed.
<!-- COCHRANBLOCK-BRAND-FOOTER:START - generated by cochranblock/scripts/brand-stamp.sh -->

---

<sub>&#9656; **THE COCHRAN BLOCK, LLC** &#183; CAGE `1CQ66` &#183; UEI `W7X3HAQL9CF9` &#183; UNLICENSE &#183; [cochranblock.org](https://cochranblock.org)</sub>
<!-- COCHRANBLOCK-BRAND-FOOTER:END -->
